{
    "name": "fullstack-portfolio-deployment",
    "version": "1.0.0",
    "description": "Deployment configuration for the portfolio",
    "scripts": {
        "start": "npm run start --prefix frontend",
        "build": "npm run build --prefix frontend",
        "install-all": "npm install --prefix frontend"
    }
}